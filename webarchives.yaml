version: '1.0'

# all known web archives
webarchive_index:
  - 'webarchives/*.yaml'

# if a web archive advertises its api, it could be added here, ex:
# - http://webarchive.example.com/wam.yaml

# see WAM.md for more info

